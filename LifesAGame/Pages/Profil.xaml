<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LifesAGame.Pages.Profil"
             Title="Profil">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="80*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Grid.Column="1" BackgroundColor="White">
            <ProgressBar x:Name="xpBar" Progress="0" ProgressColor="Green"/>
        </Grid>

        <Grid Grid.Row="1">
            <Button x:Name="updateProgress" Text="Finished Task" Clicked="updateProgress_Clicked" Margin="30"/>
        </Grid>

        <Grid Grid.Row="2" BackgroundColor="Purple">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="GetBack" IsEnabled="True" Text="Back" Grid.Column="1" HorizontalOptions="Center" VerticalOptions="Center"
                    Clicked="GetBack_Clicked" WidthRequest="120"/>
        </Grid>
    </Grid>
</ContentPage>